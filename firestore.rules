rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /performers/{document=**} {
      allow read, write: if true; // VERY PERMISSIVE - FOR TESTING ONLY
    }
    match /customers/{document=**} {
      allow read, write: if true; // VERY PERMISSIVE - FOR TESTING ONLY
    }
    match /reviews/{document=**} {
      allow read, write: if true; // VERY PERMISSIVE - FOR TESTING ONLY
    }
    // Keep other rules as is, or restrict with 'if false'
  }
}